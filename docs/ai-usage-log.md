# AI 활용 로그

작업하면서 즉시 기록한다. 형식: 날짜 / 도구 / 무엇을 시켰나 / 결과물.
외부 에셋(이미지·사운드·폰트 등)을 쓰는 순간 출처·라이선스를 여기에 함께 기록한다.

## 2026-07-31

- **Claude Code (Fable 5)** — 프로젝트 목표·범위 정의 및 요구사항 분석.
- **Claude Code (Fable 5)** — 게임 컨셉 브레인스토밍(클래리파잉 질문 → 접근안 비교 → 설계 확정).
  산출물: `docs/superpowers/specs/2026-07-31-dungeon-core-design.md`
  주요 결정: 시드 결정론 기반 던전 코어 거래/공유, 탑다운 액션, PixiJS+자작 엔진(로직/렌더 분리).
- **Claude Code (Fable 5)** — 개발 루프 세팅: GitHub 레포 생성, Vite+TS+PixiJS 스캐폴드,
  CI(테스트+빌드) → GitHub Pages 자동 배포 파이프라인 구축, 배포 검증.
  산출물: https://kws0109.github.io/seedcore/

## 2026-08-01

- **Claude Code (Fable 5)** — 회고록 자동화 스킬(`.claude/skills/wiki-retrospective`) 제작.
  섹션 완료 시 GitHub 위키에 Why/How/Flow 회고를 분류별로 기록하는 워크플로를 스킬화하고,
  첫 회고("개발 루프 구축")를 실제 작성해 절차를 검증.

- **Claude Code (Fable 5)** — 전투 프로토타입 구현 계획(TDD, 11개 태스크) 작성 후 인라인 실행.
  시뮬레이션(이동·슬래시·대시·추적 몬스터·히트스톱)과 결정론 테스트 17개, 렌더러·이펙트·HUD,
  배포까지 전 과정 AI 단독 수행. 아트는 전부 코드 드로잉(외부 에셋 0개).

- **OpenAI gpt-image-1** — 스프라이트·타일 7종 생성 (기사, 구울, 해골 궁수, 브루트, 돌바닥,
  횃불, 코어 크리스탈). 프롬프트 전문은 `assets/manifest.json`에 버전 관리됨.
  스타일 템플릿 접두 방식으로 에셋 간 일관성 확보. 생성 스크립트: `tools/gen-assets.mjs`.
- **Claude Code (Fable 5)** — 아트 방향 전환(중세 다크판타지), 에셋 파이프라인 구축,
  스프라이트 렌더러 전환 + 광원·비네트 연출. 렌더 레이어 누락 버그를 브라우저 자동 검증으로 발견·수정.

- **Claude Code (Fable 5)** — 던전 생성 섹션: 결정론적 생성기(방·복도·스폰·드롭 사전 롤링),
  몬스터 3종 AI·투사체·벽 충돌·드롭·클리어 루프를 TDD로 구현(테스트 33개), 배포까지 단독 수행.
  클리어 불가능 던전 차단을 위한 플러드필 도달 가능성 테스트 포함.

## 2026-08-02

- **Claude Code (Fable 5)** — 생성기 개편: RNG 스트림 분리(deriveSeed), 유기적 지형
  (블롭 방·랜덤워크 복도·고립 프루닝), 바이옴 3종(프리셋·팔레트·광원) 구현. 테스트 39개.
- **OpenAI gpt-image-1** — 바이옴 에셋 4종 추가 생성(동굴/해저 바닥, 발광 버섯, 심해 산호).
  전역 스타일 템플릿이 바닥 텍스처와 충돌하는 실패 사례를 바닥 전용 템플릿 분리로 해결
  (프롬프트는 `assets/manifest.json`의 `styles.floor`).

- **Claude Code (Fable 5)** — 코어 시스템: 코어 코드 인코딩(체크섬)·던전 요약·감정가,
  메타 저장(localStorage), 시장 패널(구매·판매·코드 공유·강화) 구현. 테스트 49개.
  브라우저 자동화로 구매→입장→소모 등 실제 DOM 시나리오 검증.

- **Claude Code (Fable 5)** — 은신처 허브: 수제 타일맵, 스테이션 4종(E 상호작용),
  코어 삽입→포탈 진입 루프, 클리어/사망 귀환 흐름 재배선. 테스트 51개.
- **OpenAI gpt-image-1** — 스테이션 에셋 4종 (오벨리스크 장치·궤·모루·후드 상인).

## 2026-08-03

- **Claude Code (Fable 5)** — 프로시저럴 애니메이션: 그림자 레이어, 걸음 스쿼시·홉·런지,
  피격 방향성 스쿼시, 시체 페이드. 프레임 스프라이트 없이 코드 모션만으로 구현
  (렌더러 전용 변경, 시뮬레이션 무변경).

- **Meshy AI** — 3D 파이프라인 구축(진행 중): 스프라이트 레퍼런스 → 이미지-3D → 리메시 →
  자동 리깅 → 애니메이션 클립(대기·걷기·공격). 기사 모델+검 정적 메시 생성.
  검·망토를 리깅에서 분리하는 설계로 자동 리깅의 스키닝 오류(검 휨, 망토-손 융착)를 해결.
- **Claude Code (Fable 5)** — Meshy API 파이프라인 스크립트, three.js 캡처 뷰어(본 부착·
  월드 스케일 보정), Playwright 무인 시각 검증 루프 구축.

- **Claude Code (Fable 5) + Meshy** — 검 그립 실측 보정(본 축 스크린샷 이분탐색), 슬래시 클립
  교체, 시트 캡처 자동화(캐릭터/무기 분리 패스), 플레이어 4방향 프레임 애니메이션 인게임 통합.

- **Meshy AI + Claude Code (Fable 5)** — 몬스터 3종(구울·궁수·브루트) 3D 파이프라인 확장:
  직립 클린 레퍼런스 전략으로 비휴머노이드 리깅 통과, 무기 분리 부착(활=왼손, 철퇴),
  8방향 시트 15장 캡처·통합. 범용 애니메이션 로더로 리팩터링.

- **Claude Code (Fable 5)** — 강체 충돌 분리(관통·스택 차단) 및 몬스터 AI(시야 기반 어그로,
  피격 경보 전파, 타일 A* 길찾기 — 결정론적 타이브레이크) 구현. 테스트 64개.

- **Claude Code (Fable 5)** — 비인지 배회 행동: 홈 반경 배회를 (시드·개체·에포크) 순수 해시로
  구현해 결정론 유지. 테스트 67개.

- **Claude Code (Fable 5)** — 몬스터 방향-이동 연동(문워크 해소): 상태별 방향 규칙 분리,
  이동 샘플 24건 자동 검증(92% 정합, 잔여는 방향 전환 경계).

- **Claude Code (Fable 5)** — 겹침 잔상 진단·해소: 가설 3개 순차 기각(모델 복사본·시트 잔상·
  렌더 클리어) 후 실원인(충돌원 대비 스프라이트 과대 + 깊이 정렬 부재) 확정.
  분리 간격 1.35배 + y-정렬 엔티티 레이어 도입.

- **Claude Code (Fable 5)** — 잔상 2차 원인(클립 스래싱) 규명: 공간 가설 전부 기각 후
  시간축 진단(임계값 진동 → walk/idle 프레임 교차). 히스테리시스 도입, 전환 빈도
  실측(50ms 샘플링)으로 자연 전환만 남음을 정량 확인.

- **Claude Code (Fable 5)** — 전 에셋 WebP 파이프라인(sharp): 로드 20.5MB → 2.7MB (87% 절감),
  시각 무손실 수준. 시트 재캡처 후 재실행 가능한 변환 스크립트로 도구화.

- **Claude Code (Fable 5)** — 전투 체계 재정립: 클릭 공격을 제거하고 근접 시 커서 방향 자동 공격으로
  전환. 물리 테스트의 자동 공격 간섭을 시선 반전으로 배제, 발동·비발동 회귀 테스트 추가 (69개).

- **Claude Code (Fable 5)** — 디아블로식 시점 전환: 월드 y-압축 축측 투영(캐릭터 캡처 카메라
  38°와 일치), 빌보드 역보정, 커서 역투영(수치 검증), 입체 벽(상판·입면·접지 AO·오클루전).
  렌더 전용 변경으로 시뮬레이션·결정론 무변경. 벽 텍스처는 절차 생성(외부 에셋 0).

- **OpenAI gpt-image-1** — 바이옴별 벽면 텍스처 3종 생성(석조 벽돌·동굴 암벽·심해 현무암).
  프롬프트는 `assets/manifest.json`의 `styles.wall` 템플릿. 수평 상판에 깔면 바닥으로
  오독되는 실패를 거쳐 수직 입면 전용으로 적용 범위를 확정.
- **Claude Code (Fable 5)** — 카메라 줌(1.3×) 및 벽 질감 통합, 상판/입면 재질 분리 원칙 수립.

- **Claude Code (Fable 5)** — 시야 시스템: 거리+LoS 렌더 게이팅(몬스터·투사체·드롭),
  화면 고정 타원 fog, 시야 스탯(visionMul) 파라미터화, 자동 공격 LoS 게이트(테스트 70개).
  시뮬레이션-렌더 분리 원칙 유지로 결정론 무영향. 카메라 줌은 사용자 브라우저 확대
  상태를 실측(2.5×)해 3.25배로 확정하는 방식으로 협업.
- **Claude Code (Fable 5)** — 줌 수치화: 고정 상수 대신 휠 조절(1.2~4, 기본 2.2)·배율 토스트·
  localStorage 유지로 전환. 사용자가 직접 체감으로 값을 탐색할 수 있는 구조.

## 외부 에셋·오픈소스

| 항목 | 용도 | 라이선스 |
|---|---|---|
| PixiJS v8 | 렌더링 | MIT |
| Vite / Vitest / TypeScript | 빌드·테스트 | MIT/Apache-2.0 |
| OpenAI gpt-image-1 생성 이미지 | 게임 스프라이트·타일 전부 | 생성물 소유권은 이용약관에 따라 사용자 귀속. 프롬프트·생성 내역 공개 |
